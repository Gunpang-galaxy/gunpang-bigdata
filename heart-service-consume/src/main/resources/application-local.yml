spring:
  application:
    name: heartbeat-consume
  kafka:
    bootstrap-servers: ${bootstrap-local-server}
    streams:
      application-id: heartbeat-consume

logging:
  level:
    com.gunpang.heartservice: DEBUG